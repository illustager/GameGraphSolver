# Tests

## Running Tests

In the `tests/` directory, run all tests using the following command:

```bash
make run-all
```

Or run a single test:

```bash
make run-test01-map
```

Clean up generated files:

```bash
make clean
```

## Test Cases Description

See [CASES.md](CASES.md).
